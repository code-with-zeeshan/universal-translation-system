# Decoder-service specific add-ons
# Base and serve should be installed alongside this if building the decoder image:
#   -r base.txt
#   -r serve.txt

# Event loop optimization (non-Windows)
uvloop>=0.22.1; platform_system != 'Windows'
# Faster JSON
orjson>=3.11.9
# Retry helpers used in node/HTTP ops
tenacity>=9.1.4
