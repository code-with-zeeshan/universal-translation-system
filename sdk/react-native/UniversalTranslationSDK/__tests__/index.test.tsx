import { UniversalTranslationModule } from '../src/index';

describe('UniversalTranslationModule', () => {
  it('should be defined', () => {
    expect(UniversalTranslationModule).toBeDefined();
  });
});
