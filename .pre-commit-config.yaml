repos:
  - repo: local
    hooks:
      - id: check-unsafe-yaml-load
        name: Block unsafe yaml.load usage
        entry: python scripts/precommit/check_unsafe_yaml.py
        language: system
        types: [python]
        stages: [commit, push]