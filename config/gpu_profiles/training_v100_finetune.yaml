# NOTE: Archived — NOT loaded by the trainer.
# Fine-tune variant of the V100 config. The trainer has no separate fine-tune
# strategy; override via CLI: --learning-rate 1e-5 --num-epochs 3
# Base V100 settings in trainer.py "v100" strategy:
#   batch_size=32, accumulation=4, lr=3e-4, dtype=float16
training:
  lr: 1e-5
  temperature: 3.0
