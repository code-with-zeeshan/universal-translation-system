# NOTE: Archived — NOT loaded by the trainer.
# This config uses an outdated schema (flat fields, no nested `training:` key matching RootConfig).
# The trainer handles multi-GPU by taking the single-GPU strategy, setting
# use_distributed=True, use_ddp=True, and multiplying batch_size by gpu_count.
# See trainer.py lines 711-722.
