# NOTE: Archived — NOT loaded by the trainer.
# This config uses an outdated schema (flat fields, no nested `training:` key matching RootConfig).
# The trainer uses a hardcoded strategy dict in _determine_optimal_strategy().
# For equivalent behavior, the fallback for ≥20GB GPUs is MID_RANGE_SINGLE.
# See trainer.py lines 697-709 for the memory-based fallback logic.
