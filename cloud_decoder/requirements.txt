# cloud_decoder/requirements.txt (canonicalized)
# Include canonical dependency sets to avoid drift
-r ../requirements/base.txt
-r ../requirements/serve.txt
-r ../requirements/decoder.txt

# Dev/test (optional in runtime images, harmless if present)
pytest>=7.4.0
pytest-asyncio>=0.21.0